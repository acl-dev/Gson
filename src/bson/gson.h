namespace acl
{
    //group_t
    
    bool gson (const group_t&obj, bson_t &$bson);
    bool gson (const group_t*obj, bson_t &$bson);
    
    
    //user_t
    
    bool gson (const user_t&obj, bson_t &$bson);
    bool gson (const user_t*obj, bson_t &$bson);
    
    
}///end of acl.
